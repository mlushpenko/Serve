<div class="row-fluid">
<div class="span3">
<h1>Categories</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @categories.each do |category| %>
  <tr>
    <td><%= link_to (category.name + " (#{category.books.count})"), :controller => 'categories', :action => 'index', :id => category %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Category', new_category_path %>
</div>
<div class="span9">
<%= render :template => 'books/index' %>
</div>
</div>
