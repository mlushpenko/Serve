<div class = "row-fluid">

<div class = "span3">
  <div class="well sidebar-nav">
  <%= link_to 'Admin control', admin_root_path %>
    <ul class="nav nav-list">
      <li class = "nav-header">Categories</li>
  
      <% @categories.each do |category| %>
        <li><%= link_to (category.name + " (#{category.books.count})"), :controller => 'categories', :action => 'index', :id => category %></li>
      <% end %>
       
    </ul>
  </div>
</div>

<div class = "span9 well well-small">
  <%= render :template => 'books/index' %>
</div>

</div>
